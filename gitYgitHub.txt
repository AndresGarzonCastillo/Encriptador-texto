crear un nuevo repositorio:
git config --global user.name "mi nombre"
git config --global user.email "mi email"
voy a la ruta de mi proyecto en mi pc
git init
git remote add origin https://github.com/AndresGarzonCastillo/Encriptador-texto
git branch -M main
git push -u origin main

actualizar versiones:
gid add .
gid commit -m "Agregar txt git"
git push -u origin main

ver estados y acciones en el git:
git status
git log

